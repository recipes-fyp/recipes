{% extends 'base.html.twig' %}

{% block title %}Edit Collection{% endblock %}

{% block body %}
    <h1>Edit Collection</h1>

    {{ include('coll/_form.html.twig', {'button_label': 'Update'}) }}
	<div class="btn-group">
	    <a href="{{ path('coll_index') }}">Back</a>

	    {{ include('coll/_delete_form.html.twig') }}
	</div>
{% endblock %}